<UserControl x:Class="ObserverLm.UserControls.StatusControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ObserverLm"
             xmlns:userControls="clr-namespace:ObserverLm.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
   

        <Grid>

      
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <userControls:CurlControl Grid.Row="0" Margin="5,5,5,0"  x:Name="CurrentControlCore" />
        <!-- json текстовое поле --> 
        <TextBox Grid.Row="1" 
                 Margin="5"
                 x:Name="TextBoxStatus"
                 Padding="20"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontSize="20"
                 FontWeight="Bold"
                 FontFamily="Consolas, Courier New, monospace"
                 ToolTip="JSON-ответ от сервера" />

       
       
    </Grid>
</UserControl>
