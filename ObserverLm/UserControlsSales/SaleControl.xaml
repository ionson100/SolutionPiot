<UserControl x:Class="ObserverLm.UserControlsSales.SaleControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ObserverLm.UserControlsSales"
             xmlns:userControls="clr-namespace:ObserverLm.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
           
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

       


        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10,5,10,15">
            <Label Width="80" HorizontalAlignment="Left" VerticalAlignment="Center" FontWeight="Bold">CODE:</Label>
            <TextBox x:Name="InputTextBox" 
                  Width="700" 
                  VerticalAlignment="Center" 
                  Style="{StaticResource EditOrder}"
                  ToolTip="Введите код для проверки"/>
            <Button x:Name="CheckButton" 
                 Margin="20,0"
                 Width="150"
                 Content="Проверить" 
                 Style="{StaticResource ButtonHeadPaneStyle}"
                 Click="CheckButton_OnClick"
                 IsDefault="True"/>
        </StackPanel>

        <userControls:CurlControl Grid.Row="1"  x:Name="CurlControlCore" />



        <TextBox Grid.Row="2" 
              Margin="0,5,0,0"
              x:Name="OutputTextBox"
              Padding="20"
              IsReadOnly="True"
              AcceptsReturn="True"
              TextWrapping="Wrap"
              VerticalScrollBarVisibility="Auto"
              HorizontalScrollBarVisibility="Auto"
              FontSize="20"
              FontWeight="Bold"
              FontFamily="Consolas, Courier New, monospace"
              ToolTip="JSON-ответ от сервера"/>
        <ProgressBar Grid.Row="2" Name="LoadingBar"
                  IsIndeterminate="True" 
                  Width="50" Height="50"
                  Visibility="Collapsed" />
    </Grid>
</UserControl>
