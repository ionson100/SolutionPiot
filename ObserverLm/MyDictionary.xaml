<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <SolidColorBrush x:Key="LightGreyBrush" Color="#c0c0c0" />
    <SolidColorBrush x:Key="MidGreyBrush" Color="#808080" />
    <SolidColorBrush x:Key="MidGreyBrushSelect" Color="#a8a3a3" />
    <SolidColorBrush x:Key="DarkGreyBrush" Color="#404040" />
    <SolidColorBrush x:Key="DarkGreyBrushFocus" Color="CadetBlue" />

    <Style x:Key="ButtonHeadPaneStyle" TargetType="{x:Type Button}">

        <Style.Triggers>
            <Trigger Property="IsFocused" Value="true">
                <Setter Property = "Foreground" Value="LightGoldenrodYellow"/>
            </Trigger>
        </Style.Triggers>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Grid Background="{StaticResource MidGreyBrush}">


                        <Border x:Name="TopLeftBorder" BorderBrush="{StaticResource LightGreyBrush}"
                                BorderThickness="3,3,0,0" />
                        <Border x:Name="BottomRightBorder" BorderBrush="{StaticResource DarkGreyBrush}"
                                BorderThickness="0,0,3,3" />

                        <Grid x:Name="Grid1" Margin="3">
                            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                                <ContentPresenter Margin="{TemplateBinding Padding}" />
                            </Grid>

                        </Grid>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Grid1" Property="Background" Value="Red" />
                            <Setter TargetName="TopLeftBorder" Property="BorderBrush"
                                    Value="{StaticResource DarkGreyBrush}" />
                            <Setter TargetName="BottomRightBorder" Property="BorderBrush"
                                    Value="{StaticResource LightGreyBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Grid1" Property="Background" Value="DarkKhaki" />
                            <Setter TargetName="TopLeftBorder" Property="BorderBrush"
                                    Value="Bisque" />
                            <Setter TargetName="BottomRightBorder" Property="BorderBrush"
                                    Value="AntiqueWhite" />
                        </Trigger>
                    </ControlTemplate.Triggers>


                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Margin" Value="2" />
        <Setter Property="MinHeight" Value="50" />
        <Setter Property="MinWidth" Value="50" />
        <Setter Property="FontSize" Value="20" />
        <Setter Property="Padding" Value="5" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Foreground" Value="White" />

    </Style>

    <Style x:Key="ButtonHeadPaneSelectStyle" TargetType="{x:Type Button}">

        <Style.Triggers>
            <Trigger Property="IsFocused" Value="true">
                <Setter Property = "Foreground" Value="LightGoldenrodYellow"/>
            </Trigger>
        </Style.Triggers>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Grid Background="{StaticResource MidGreyBrushSelect}">


                        <Border x:Name="TopLeftBorder" BorderBrush="{StaticResource LightGreyBrush}"
                            BorderThickness="3,3,0,0" />
                        <Border x:Name="BottomRightBorder" BorderBrush="{StaticResource DarkGreyBrush}"
                            BorderThickness="0,0,3,3" />

                        <Grid x:Name="Grid1" Margin="3">
                            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                                <ContentPresenter Margin="{TemplateBinding Padding}" />
                            </Grid>

                        </Grid>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Grid1" Property="Background" Value="Red" />
                            <Setter TargetName="TopLeftBorder" Property="BorderBrush"
                                Value="{StaticResource DarkGreyBrush}" />
                            <Setter TargetName="BottomRightBorder" Property="BorderBrush"
                                Value="{StaticResource LightGreyBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Grid1" Property="Background" Value="DarkKhaki" />
                            <Setter TargetName="TopLeftBorder" Property="BorderBrush"
                                Value="Bisque" />
                            <Setter TargetName="BottomRightBorder" Property="BorderBrush"
                                Value="AntiqueWhite" />
                        </Trigger>
                    </ControlTemplate.Triggers>


                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Margin" Value="2" />
        <Setter Property="MinHeight" Value="50" />
        <Setter Property="MinWidth" Value="50" />
        <Setter Property="FontSize" Value="20" />
        <Setter Property="Padding" Value="5" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Foreground" Value="White" />

    </Style>





    <Style x:Key="EditOrder" TargetType="TextBox">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                        <ScrollViewer x:Name="PART_ContentHost" Focusable="false"
                                      HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Opacity" TargetName="border" Value="0.56" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="BorderBrush" TargetName="border" Value="#FF7EB4EA" />
                        </Trigger>
                        <Trigger Property="IsFocused" Value="true">
                            <Setter Property="Background" Value="LightGreen" />
                            <Setter Property="BorderBrush" TargetName="border" Value="Red" />
                        </Trigger>
                        <Trigger Property="IsReadOnly" Value="True">
                            <Setter Property="Background" Value="Bisque" />
                            <Setter Property="BorderBrush" TargetName="border" Value="Red" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="FontSize" Value="30" />
    </Style>




   


</ResourceDictionary>